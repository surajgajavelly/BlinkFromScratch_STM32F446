# BlinkFromScratch_STM32F446
Bare‑metal LED blink project for STM32F446RE — no HAL, no CMSIS, just startup code, linker script, and direct register access. A deep dive into how the microcontroller boots, initializes memory, and toggles GPIO at the lowest level.
